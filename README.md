# ReactNativeIntegrationSample

## Setup

* Install React Native dependencies on project root: `npm install`;
* Add the library's `.aar` file in `android/app/libs` folder;
* `./gradlew clean` the project and sync it with Gradle files.
